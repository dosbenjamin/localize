{
  "name": "localize",
  "version": "0.0.1",
  "scripts": {
    "dev:web": "turbo dev --filter web",
    "dev:ui": "turbo dev --filter ui",
    "dev": "turbo dev",
    "build:web": "pnpm --filter web build",
    "build": "pnpm -r build",
    "lint:web": "turbo lint --filter web",
    "lint:ui": "turbo lint --filter ui",
    "lint:eslint-config": "turbo lint --filter eslint-config",
    "lint:theme": "turbo lint --filter theme",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "prettier": "^2.8.4",
    "prettier-plugin-tailwindcss": "^0.2.3"
  }
}
