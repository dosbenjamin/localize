{
  "name": "localize",
  "version": "0.0.1",
  "scripts": {
    "storybook": "pnpm --filter ui storybook",
    "dev:front": "pnpm --filter front dev",
    "dev": "pnpm -r dev",
    "build:front": "pnpm --filter front build",
    "build": "pnpm -r build",
    "lint:front": "pnpm --filter front lint",
    "lint": "pnpm -r build"
  },
  "devDependencies": {
    "prettier": "^2.8.1",
    "prettier-plugin-tailwindcss": "^0.2.1"
  }
}
