{
  "name": "localize",
  "version": "0.0.1",
  "scripts": {
    "storybook": "pnpm --filter ui storybook",
    "dev:web": "pnpm --filter web dev",
    "dev": "pnpm -r dev",
    "build:web": "pnpm --filter web build",
    "build": "pnpm -r build",
    "lint:web": "pnpm --filter web lint",
    "lint:eslint-config": "pnpm --filter eslint-config lint",
    "lint:theme": "pnpm --filter theme lint",
    "lint:ui": "pnpm --filter ui lint",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "prettier": "^2.8.4",
    "prettier-plugin-tailwindcss": "^0.2.3"
  }
}
